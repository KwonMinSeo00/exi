/*
    Scaffold: Resource payload boundaries and type-specific layouts.
    Include icons, cursors, bitmaps, dialogs, menus, strings, accelerators,
    fonts, message tables, and custom resource data.
*/
